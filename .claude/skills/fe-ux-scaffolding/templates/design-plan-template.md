# План UX: {Название задачи}

## Дерево компонентов
{Корневой компонент}
├── {Компонент состояния "успех"}
│   └── {Дочерний компонент} → переиспользует <{КомпонентUIkit}>
├── {Компонент состояния "пусто"} → переиспользует <{КомпонентUIkit}>
└── {Компонент состояния "загрузка"} → переиспользует <{КомпонентUIkit}>

## Используемые design tokens
- {токен}: {значение/переменная}

## Новые компоненты (обоснование)
- {компонент} — обоснование: {почему нет подходящего примитива в UI-kit}

## Не покрыто существующим UI-kit (риск для следующей стадии)
- {описание пробела}
